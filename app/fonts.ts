import { Bebas_Neue } from "next/font/google";

// Shared display face for bold headlines on the YakiGang side of the site.
export const bebasNeue = Bebas_Neue({ weight: "400", subsets: ["latin"] });
